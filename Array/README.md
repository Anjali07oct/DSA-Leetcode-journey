# Deeclare name of function 
# take pointer arr nums, numsSize, and value val
# here j used to traverse array
# use i to store position of valid elemnt 
# if nums[i] == val  -> remove the num[i] and update new nums[]
# declare return i as new length
